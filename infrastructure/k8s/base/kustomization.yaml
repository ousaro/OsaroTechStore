apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: osarotechstore

resources:
  - namespace.yaml
  - backend-deployment.yaml
  - backend-service.yaml
  - mongodb-backup-cronjob.yaml
  - mongo-statefulset.yaml
  - mongo-service.yaml

commonLabels:
  app.kubernetes.io/part-of: osarotechstore
